import { styled } from 'config/stitches.config'

export const Chip = styled('div', {
  width: '20px',
  height: '20px',
  borderRadius: '100%',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  padding: '6px',
})
